		Todo file for libkv


- All design should be simply.

- Disk persistent.
	Refer to leveldb.

- Both .a and .so are producted.
	Move jemalloc to independent dynamic library.

- Move libkv.h to include directory.

- Add compile automatically.

- Change debug.c to kv_assert.h/kv_assert.c.

- Add oom_cb to config list(kv_init).

- kv_create support parameters list.

- Replace kv_version.h by config.h.in.

- Support kv_ask block value input.
