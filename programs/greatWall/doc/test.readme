昨晚压测damS：
发现damS的并发瓶颈在libev的accept loop上（supex框架的应用服务都是）;
webatch压测性能每分钟190万左右并发请求,但是7个读写线程cpu利用率只用20%～30%左右，主要是accept loop过来的请求不够多。
把7读写线程改成2读写线程后cpu利用率达到50%～70%及以上，但是并发量没下降，因此把节约出来的5个线程改为worker线程整体性能会更佳！

