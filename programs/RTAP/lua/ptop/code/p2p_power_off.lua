local only = require('only')
local supex = require('supex')
local utils = require('utils')
local link = require('link')
local cjson = require('cjson')

module('p2p_power_off', package.seeall)


function handle()
end
